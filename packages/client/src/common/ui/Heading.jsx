import { Heading as ChakraHeading } from '@chakra-ui/core'

export const theme = {
    Heading: {
        baseStyle: {
            fontWeight: 300,
        },
    },
}

export const Heading = ChakraHeading
